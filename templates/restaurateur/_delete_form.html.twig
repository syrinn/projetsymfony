<form method="post" action="{{ path('app_restaurateur_delete', {'id': restaurateur.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ restaurateur.id) }}">
    <button class="btn">Delete</button>
</form>
